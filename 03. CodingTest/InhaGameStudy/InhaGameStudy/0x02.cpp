#include <iostream>
#include <queue>

using namespace std;

int main()
{
	cin.tie(0);
	ios::sync_with_stdio(0);

	int n, ht;
	cin >> n;
	queue<int> que;
	cin >> ht;
	que.push(ht);
	for (int i = 1; i < n; i++)
	{
		cin >> ht;
		if(qu)
	}

	return 0;
}